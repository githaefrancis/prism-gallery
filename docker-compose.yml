version: "3.8"

services:
  web:
    build: .
    command: python3 manage.py 0.0.0.0:8004
    volumes:
      - .:/usr/src/prism_gallery/
    ports:
      - 8004:8004
    env_file:
      - ./.env
    depends_on:
      - db2
  db2:
    image: postgres:13.0-alpine
    volumes:
      - postgres_data_prism:/var/lib/postgresql/data/
    environment:
      - POSTGRES_USER=${DB_USER}
      - POSTGRES_PASSWORD=${DB_PASSWORD}
      - POSTGRES_DB=${DB_NAME}
volumes:
  postgres_data_prism:
